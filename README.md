## Digifarm MVT API integration example with openlayers

#### Dependency install

`npm install`

#### Running locally

`token=Your-digifarm-token npm run start`

Open the resultant link in the browser
